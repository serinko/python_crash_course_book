digits = list(range(0,41,2))
	# Creates list of digits in range 0,41, by 2
print (digits)
	#control print of the list
print("\n")

print(min(digits))
	# "min" function shows the smallest digit from "digits"
print("\n")

print(max(digits))
	# "max" function shows the biggest digit from "digits"
print("\n")

print(sum(digits))
	# "sum" function shows the sum value "digits"
